import { Box, Text } from "@chakra-ui/react";

const TestScreen = () => {
  return (
    <Box>
      <Text>This is a test screen</Text>
      <Text>No components, no Redux, no routing</Text>
    </Box>
  );
};

export default TestScreen;